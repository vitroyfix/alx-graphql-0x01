# ALX GraphQL 0x01 - Rick and Morty App

This project is a Next.js application that uses Apollo Client to query the Rick and Morty GraphQL API.

## Project Structure

- `graphql/` → Apollo Client setup and queries
- `pages/_app.tsx` → Application root with ApolloProvider
- `components/common/` → Shared components
